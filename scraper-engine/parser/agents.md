follow a the list.json 
if any src in prasing stage 
then create new script to parse the src in scripts folder in [src-name].ty which take data as input cmd line argument (default today)
output the data in json in data/[src-name]/[date].json