{
  "name": "prediction-engine",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "dev": "./.venv/bin/uvicorn app.main:app --host 0.0.0.0 --reload",
    "start": "./.venv/bin/uvicorn app.main:app --host 0.0.0.0",
    "build": "./.venv/bin/python -m compileall app"
  }
}
