{
  "name": "ajrasakha-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "turbo run dev --parallel --continue --filter=consumer-portal --filter=apmc-portal --filter=server --filter=prediction-engine --filter=endpoint-discovery --filter=scraper-engine-ts",
    "dev:web": "turbo run dev --filter=consumer-portal --filter=apmc-portal",
    "dev:api": "turbo run dev --filter=server --filter=prediction-engine --filter=endpoint-discovery --filter=scraper-engine-ts",
    "build": "turbo run build --filter=consumer-portal --filter=apmc-portal --filter=server --filter=prediction-engine --filter=endpoint-discovery --filter=scraper-engine-ts",
    "lint": "turbo run lint --filter=consumer-portal --filter=apmc-portal",
    "test": "turbo run test --filter=consumer-portal --filter=apmc-portal",
    "setup:env": "test -f .env || (cp .env.example .env 2>/dev/null && echo 'Created .env from .env.example - please configure values') || echo 'WARNING: .env.example not found. Create .env manually with required values.'",
    "setup:js": "pnpm install",
    "setup:python": "python3 -m venv pridiction-engine/.venv && pridiction-engine/.venv/bin/pip install -r pridiction-engine/requirements.txt && python3 -m venv scraper-engine/endpoint-discovery/.venv && scraper-engine/endpoint-discovery/.venv/bin/pip install -r scraper-engine/endpoint-discovery/requirements.txt && scraper-engine/endpoint-discovery/.venv/bin/playwright install chromium",
    "setup": "pnpm run setup:env && pnpm run setup:js && pnpm run setup:python",
    "check": "pnpm run lint && pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
